# AGENTS.md

> 本文件是给 AI 编码代理的「工作说明书」。**每次新会话开始时先读它。**
> 它分两部分：**占位区**（每个项目要填，标了 `〈…〉`）和**通用纪律**（跨项目通用，一般不用改）。
> 复用方式：把本文件拷进新仓库、改名为 `AGENTS.md`，只改第 1–2 节，其余照用。

---

## 0. 开始之前（每次会话必读）

1. 读本文件；若仓库有 `docs/`，先读其中的需求 / 技术设计 / 计划类文档。
2. 动手前先弄清「这次要做的最小一件事是什么」。
3. **不确定就问，不要猜**——需求、取舍、范围有歧义时，停下来确认，而不是自由发挥。

---

## 1. 项目概览

- **这是什么**：基于 OpenMAIC 二开的企业课程与考核平台，面向企业管理员和学员，提供 AI 课程生成/编辑、课程发布、学习、课后测评、阶段考试和数据看板。
- **技术栈**：TypeScript / Next.js App Router / React / Zustand / Dexie；计划新增 PostgreSQL、Drizzle ORM、Aliyun OSS、签名 session cookie、Host API Key/Secret；课程 DSL、导入与渲染复用 `@openmaic/*` workspace 包。
- **关键文档**：`docs/PRD.md`、`docs/technical-design.md`、`docs/plan/SLICE-OVERVIEW.md`、`docs/plan/SLICE-00.md` 至 `docs/plan/SLICE-08.md`。
- **当前阶段**：二开需求与技术方案已完成 v0.5 梳理，建议从 `SLICE-00` 基础设施开始，再按 `SLICE-07 -> SLICE-01 -> SLICE-08 -> SLICE-03 -> SLICE-04 -> SLICE-05` 推进主链路。

---

## 2. 环境与常用命令

```powershell
# 安装依赖
pnpm.cmd install

# 本地运行
pnpm.cmd dev

# 构建
pnpm.cmd build

# 跑测试
pnpm.cmd test

# 代码检查 / 格式化
pnpm.cmd lint
pnpm.cmd check
```

> 提交前必须能跑通的命令：**测试** 与 **lint**。

---

## 3. 工作方式（最重要）

- **小步**：一次只做一个小任务，做到「可 review、可测试、可回滚」的颗粒度；不要一次性铺开大改。
- **先想后写**：动手前先说清「打算改/新建哪些文件、接口怎么设计」，等确认再写代码。
- **不擅自扩大范围**：只做被要求的事。顺手发现的其它问题，**记下来报告**，不要顺手一起改。
- **遵循现有风格**：新代码要像周围代码——命名、注释密度、错误处理、目录习惯都对齐，不引入个人风格。
- **复用优先**：先找现有的工具函数/组件/模式，不要重复造轮子或平行实现。
- **新依赖要先问**：引入任何新的第三方库前，先说明理由并征得同意。

---

## 4. 测试纪律

- 新增或修改行为，**配套测试**（优先 TDD：先写测试再实现）。
- 提交前跑完整 **测试 + lint**，**全绿**才算这步完成。
- **不要为了让测试通过而篡改断言**——除非需求确实变了，并说明原因。
- 测试要覆盖核心逻辑和边界/异常路径，不只测 happy path。

---

## 5. 代码与架构约定

- 类型/契约清晰；公共边界（API、模块接口）显式定义，不靠隐式约定。
- **把易变与稳定的东西隔离**：配置与逻辑分离，外部差异（如第三方平台/支付/存储）集中到适配层，业务代码不直接耦合细节。
- 函数/模块单一职责；命名表达意图，避免无意义缩写。
- 注释解释「为什么」，不复述「做了什么」。
- 错误要显式处理并给出可理解的信息，不静默吞掉。

---

## 6. Git 与提交

- **一个任务一次提交**，提交信息说清「做了什么 / 为什么」。
- 不在主分支上直接做大改——按需先开分支。
- **未经同意：不 `push`、不 force-push、不改写已推送的 git 历史。**
- 提交前确认 **没有把密钥、`.env`、凭证、大型二进制等不该入库的文件纳入**。

---

## 7. 安全与边界

- **绝不提交**密钥、token、密码、`.env`；敏感配置一律走环境变量，并在 `.env.example` 留占位。
- **破坏性或不可逆操作先确认**：删除文件/数据、改动线上配置、迁移数据库、对外发送或发布等。
- **不臆造**文件路径、API、库或函数的存在——引用前先核实它真的存在。
- 涉及用户数据/隐私时，遵循最小收集原则，按项目合规要求处理。

---

## 8. 沟通与汇报

- **如实汇报**：测试失败就说失败并贴出输出；某步跳过就说跳过；做完并验证过才说「完成」，不粉饰、不含糊。
- 给结论而非穷举选项；需要决策时给出**推荐**，并说明取舍。
- 遇到阻塞（缺信息、权限不足、依赖冲突）立即说明，而不是绕路硬上或假装完成。

---

## 9. 「完成」的定义（Definition of Done）

一项任务满足以下全部才算完成：

- [ ] 功能实现完整，不是半成品；没有假装完成的遗留 `TODO`。
- [ ] 配套测试已写，**测试 + lint 全绿**。
- [ ] 自己看过 diff，确认没有越界改动、没有调试残留。
- [ ] 相关文档/注释已同步更新。
- [ ] 改动颗粒度适中、可被 review。

---

## 10. 文档维护

> 适用于仓库内的需求 / 设计 / 计划类文档（如 `docs/` 下的 PRD、技术设计、开发计划）。

- **单一来源，引用而非复制**：同一事实只在它的「主人」文档定义一次，其它文档用「链接 + 章节号」引用，绝不复制正文。各文档的归属边界见 `docs/README.md`（若有）。
- **谁是主人**：做什么 → 需求文档(PRD)；怎么做 → 技术设计；何时做 / 什么顺序 → 开发计划。
- **改动自上而下传播**：变更先改上游主人文档，再往下游推（什么 → 怎么 → 何时）；**不要先改代码、文档回头补**。
- **需求变更先过闸门**：新需求先做影响分析，再决定 收 / 推迟 / 拒，**先改文档后写代码**。没进文档的需求等于不存在。
- **版本可追溯**：每次变更，主文档 bump 版本号 + 变更记录加一条（改了什么、为什么）；下游文档更新「对应版本」与日期。
- **稳定与易变分离**：AGENTS.md 放稳定的「规则 / 习惯」，不放易变的「事实 / 状态」——技术栈细节、文档归属地图、当前进度等留在各自主人文档，本文件只放指针。
