请基于我刚才生成的五张企业管理后台 UI 图进行第二轮整体修改。不要只修改其中一张；请把五张图作为同一个产品、同一套设计系统中的五个页面统一重做。

目标不是制作一个通用 SaaS Admin 演示模板，而是为一个真实的“企业培训运营台”设计后台。必须保留第一版中优秀的 SaaS 后台视觉骨架，并按照下方五个页面的产品要求组织信息。当前接口已经提供的字段应直接复用；本稿新增的社区趋势、社区统计、课程封面、考核通过率汇总和最近考核学员等数据，应被视为需要补充的正式后端合同，设计稿中不得用无法落地的随机字段或长期写死的演示数据代替。

一、输出要求

请输出 5 张互相独立但视觉完全一致的桌面端页面：

1. 数据看板
2. 课程管理
3. 考核管理
4. 社区内容
5. 用户管理

设计基准尺寸为 1280×1024。
页面不能出现横向溢出。
顶部栏不能遮挡页面标题、说明、按钮或筛选区。
所有主要内容必须从顶部栏下方完整开始。
不要生成手机界面，但布局要体现可响应式收缩的结构。

二、统一品牌与设计系统

1. 将所有“SaaS Admin”“Management Portal”“SaaS Management Platform”替换为：
   - 品牌名称：元我智脑
   - 后台名称：管理后台
   - 副标题：企业培训运营台

2. 五张图必须使用完全相同的：
   - Logo
   - 左侧栏宽度
   - 顶部栏高度
   - 页面最大内容宽度
   - 字体体系
   - 图标风格
   - 圆角
   - 阴影
   - 边框
   - 主按钮
   - 状态徽标
   - 表格样式
   - 分页样式

3. 视觉风格：
   - 使用克制、专业、轻量的企业后台风格。
   - 页面背景为极浅蓝灰色。
   - 内容卡片使用白色背景、细边框和非常轻的阴影。
   - 主操作色使用深蓝。
   - 链接与选中状态使用较亮的品牌蓝。
   - 成功使用低饱和绿色。
   - 警告使用低饱和琥珀色。
   - 危险操作使用低饱和红色。
   - 控件圆角约 6px，内容卡片圆角约 8px，弹窗约 12px。
   - 不使用夸张渐变、玻璃拟态、霓虹效果或过度阴影。
   - 数字列使用易扫读的等宽数字效果。
   - 正文和辅助文字必须有足够对比度，不要使用过浅灰色。

4. 保留第一版的这些优点：
   - 固定左侧导航。
   - 清晰的页面标题、说明和右上角页面操作。
   - KPI 卡片。
   - 状态徽标。
   - 筛选区和列表区的层级。
   - 高信息密度表格。
   - 列表底部分页。
   - 主操作与次操作清楚区分。
   - 空状态、加载状态和错误状态有明确区域。

三、统一导航和顶部栏

左侧导航固定为以下五项，名称不得修改：

- 数据看板
- 课程管理
- 考核管理
- 社区内容
- 用户管理

左侧栏底部不要放无功能的“设置”和“帮助”入口。
左侧栏底部展示当前登录信息：
- 当前用户
- 当前角色

顶部栏保持简洁，只保留：
- 当前模块或“管理后台”标题
- 右侧账户菜单

账户菜单内容：
- 返回首页
- 退出登录

不要放以下无真实功能元素：
- 通知铃铛
- 九宫格应用中心
- 帮助按钮
- 全局搜索框
- 导出报告
- 系统告警入口

如果为了视觉完整性保留顶部搜索框，必须将其去掉交互强调并明确表现为不可用占位；优先方案是不展示。

四、页面 1：数据看板

页面标题：
数据看板

页面说明：
集中查看平台学员、课程、学习完成和考核情况。

右上操作：
- 刷新看板
- 账户菜单

首屏固定展示四张等宽 KPI 卡片，顺序不得调整：

1. 总学员数
   - 使用 learnerCount。
   - 显示整数，不展示无数据来源的环比百分比。

2. 活跃课程数
   - 建议定义为当前已发布、可供学员学习的课程数 activeCourseCount。
   - 若由课程列表派生，则必须使用 status = published 的统一口径。

3. 课程完成率
   - 使用 courseCompletionRate。
   - Tooltip：已完成人数 / 已开始学习人数。

4. 考核通过率
   - 使用 examPassRate。
   - 可在辅助文字中显示 examAttemptCount 次有效考核记录。

四张卡片下方使用明显的左右分栏，比例约为 2:1：

A. 左侧大卡片：社区活跃度

- 使用折线图或轻量面积折线图，不使用柱状图。
- 卡片标题右侧提供“周 / 月 / 年”分段切换，默认选中“月”。
- 周视图按天展示，月视图按日或周展示，年视图按月展示。
- 纵轴表示社区互动量，横轴表示时间。
- 建议至少展示一条“社区互动总量”主线；如果接口能稳定提供，也可区分“发帖”和“评论/回复”两条线，但不得用随机曲线填充正式实现。
- 折线悬停时展示日期、发帖数、评论/回复数与互动总量。
- 所需趋势合同建议为 communityActivity：period、points[]，每个点包含 date、postCount、replyCount、totalCount。
- 图表无数据时展示明确空状态，不绘制伪造趋势。

B. 右侧卡片：待处理事项

- 以纵向列表展示，每项包含严重级别色点、标题、简短说明、发生时间和一个明确的下一步操作。
- 内容可以来自待复核课程、缺少题目的考核范围、即将过期或已停用的邀请码，以及需要审核的社区内容。
- 高优先级使用红色，中优先级使用琥珀色，普通提醒使用蓝色或中性色。
- 卡片顶部可提供“查看全部”，跳转到相应管理模块。
- 不要展示 CPU、服务器负载等基础设施告警。

数据看板不再展示学习记录明细表；学习记录属于下钻数据，不占用本版看板首屏。

五、页面 2：课程管理

页面标题：
课程管理

页面说明：
维护课程从草稿到发布的全流程，并管理课程分类与可见范围。

右上操作：
- 刷新
- 分类管理
- 账户菜单

“分类管理”是清晰可见的次级按钮，打开分类管理弹窗或抽屉。分类管理需要支持：
- 查看分类名称与排序。
- 新建分类。
- 编辑分类名称与排序。
- 删除未被课程引用的分类；被引用时给出不能删除的明确原因。

课程主体必须使用带大封面的卡片网格，不使用桌面表格。参考 Stitch 第一版课程卡片的构图，但减少无意义留白，保证 1280×1024 下主内容区每行 3 张卡片，较窄宽度下自动变为 2 列或 1 列。

页面顶部筛选区：
- 搜索课程名称、描述或分类。
- 分类下拉筛选。
- 可见范围筛选：全部可见范围 / 全体可见 / 按角色可见。
- 清除筛选。

状态快捷筛选必须包含并只使用以下名称：
- 全部
- 已发布
- 草稿
- 下架
- 待复核

状态映射：
- published 显示“已发布”。
- draft 显示“草稿”。
- archived 在界面统一显示“下架”，不要同时出现“已归档”。
- generationComplete、generationStatus 或课后题状态异常时可归入“待复核”。

每张课程大封面卡片包含：
- 顶部大比例封面图，建议约 16:7 或 16:8。
- 封面右上角状态徽标：已发布 / 草稿 / 下架 / 待复核。
- 课程名称 name。
- 最多两行课程描述 description。
- 分类 categoryName。
- 可见范围：全体可见，或显示角色摘要。
- 学员数 learnerCount。
- 内容状态：已就绪 / 生成中 / 待复核。
- 最后更新时间 updatedAt。
- 一个主要操作和三点更多菜单。

课程封面需要正式数据合同 coverUrl：
- 有 coverUrl 时使用真实课程封面。
- 无 coverUrl 时使用统一的品牌化默认课程封面，不要给每张卡片随机生成无关商务图片。
- 图片加载失败时也回退到同一默认封面。

卡片主要操作：
- 修改可见范围。

更多操作：
- 草稿或下架课程：发布。
- 已发布课程：下架。
- 删除。

修改可见范围使用弹窗：
- 全体可见。
- 按角色可见。
- 角色多选。
- 取消。
- 保存。

页面必须保留：
- 分类管理弹窗或抽屉。
- 删除确认弹窗。
- 成功/失败 Toast。
- 空列表状态。
- 筛选无结果状态。
- 列表总数和分页。

不要添加 API 未定义的收藏数、评分、讲师或“必修/高级”等随意标签。

六、页面 3：考核管理

页面标题：
考核管理

页面说明：
管理阶段考核配置、题库准备情况和学员考核结果。

页面右上角只保留一个主按钮：
- 新建考核

不要在标题区放“刷新”“导出报告”或其他按钮；刷新可通过列表局部刷新或页面重新加载完成，账户操作保留在统一顶部栏中。

标题下方放置两张并排的大卡片，高度一致：

A. 左侧卡片：题库准备

- 使用简洁的题库或文档图标。
- 展示已发布课程数 publishedCourseCount。
- 展示有课后题课程数 readyCourseCount。
- 展示缺少课后题课程数 missingQuestionCourseCount。
- 当 missingQuestionCourseCount > 0 时使用琥珀色提醒，并提供“查看缺题课程”的下钻入口。
- 不要使用 Stitch 图中的“活跃考核”和“待批阅任务”概念。

B. 右侧卡片：考核通过率

- 参考 Stitch 设计稿中的圆盘百分比图，使用清晰的环形进度图。
- 圆盘中央显示 examPassRate，例如 88%。
- 旁边展示已通过人数、未通过人数或有效考核次数；这些数字必须来自正式汇总字段。
- 可以显示平均分，但仅在后端提供 averageScore 时出现；无数据时不要写死 82.4。
- 通过使用绿色，未通过使用红色，主圆环使用品牌蓝。

两张卡片下面是“考核列表”大卡片，顶部包含：
- 状态标签：全部 / 草稿 / 已发布 / 下架。
- 搜索考核名称或所属课程。
- 目标角色筛选。
- 必要时提供紧凑的筛选按钮。

考核列表按以下列展示，列名和顺序保持稳定：
- 考核名称 / 所属课程：title；第二行显示课程名称或课程范围摘要。
- 目标角色：targetRoleId 映射后的角色名称。
- 题量：questionCount。
- 通过线：passThreshold%。
- 限时：timeLimitMinutes；为空显示“不限时”。
- 状态：草稿 / 已发布 / 下架。
- 操作。

列表不再单独展示候选题数列；候选题数放在创建/编辑弹窗和题库准备信息中，避免表格过宽。

每条考核必须根据状态提供以下操作：
- 查看：查看进行该考核的最近学员信息。
- 编辑。
- 发布。
- 下架。
- 删除，仅允许符合服务端规则的状态执行。

“查看”打开抽屉或详情弹窗，展示最近参加该考核的学员列表：
- 学员 displayName。
- 角色 roleName 或 roleCode。
- 考核时间 createdAt。
- 第几次作答 attemptNumber。
- 分数 score。
- 结果 passed，显示“通过”或“未通过”。
- 用时 duration；无数据时显示“—”。
- 默认按考核时间倒序，可查看更早记录。

该功能需要正式的管理员考核记录查询合同，建议支持 examPolicyId、page、pageSize，并返回 total 与 attempts[]；不得在实现中写死 Stitch 示例学员。

新建/编辑考核使用大弹窗，分两段：

基础设置：
- 考核名称 title。
- 目标角色 targetRoleId。
- 题量 questionCount。
- 通过线 passThreshold。
- 限时 timeLimitMinutes。

题源范围：
- 分类多选 categoryIds。
- 课程多选 courseIds。
- 显示当前候选题数 candidateQuestionCount。

底部按钮：
- 取消。
- 保存草稿或保存修改。

禁止把页面设计成人工批阅队列，不展示“待批阅”“去批阅”“论文作业”或人工评分操作。

七、页面 4：社区内容

页面标题：
社区内容

页面说明：
查看社区活跃情况，并统一审核帖子、回复和弹幕内容。

页面上方固定展示三张等宽统计卡片：

1. 今日新帖
   - 字段建议为 todayPostCount。
   - 可使用帖子图标和品牌蓝色强调。

2. 今日评论
   - 统计今日新增回复/评论，字段建议为 todayReplyCount。
   - 注意这里统计的是评论或回复，不要用帖子总数代替。

3. 审核记录
   - 显示今日已完成的管理员审核操作数 todayModerationCount，或当前筛选范围内的审核记录数。
   - 辅助文字可以显示隐藏、删除、恢复等操作数量摘要。

三张统计卡片的数据应由正式社区统计接口提供，统一使用 Asia/Shanghai 自然日口径，不得写死演示数字。

统计卡片下方保留内容类型切换：
- 帖子，默认选中。
- 回复。
- 弹幕。
- 操作审计。

默认“帖子”页参考 Stitch 最新讨论主题设计，使用紧凑、清晰的纵向帖子列表，不使用传统宽表格。列表头包含标题“社区帖子”、当前结果数量和筛选/刷新操作。

每条帖子展示：
- 作者头像：如果没有真实头像字段，使用姓名首字或统一默认头像，不随机使用无关人物照片。
- 作者 displayName。
- 作者角色 roleName。
- 发布时间 createdAt。
- 标题 title。
- 正文摘要 body，最多两到三行。
- 所属课程 courseName；全局帖子显示“全局讨论”。
- 回复数 replyCount。
- 状态：可见 / 已隐藏 / 作者已删除 / 管理员已删除 / 已归档。
- 已置顶 pinned。
- 已关闭回复 locked。
- 最近审核原因 moderationReason，如果存在。

帖子筛选：
- 关键词 keyword。
- 作者用户 ID authorId。
- 课程 ID courseId。
- 内容状态 status。
- 开始时间 from。
- 结束时间 to。
- 清空筛选。
- 刷新。

每条帖子必须有审核操作：
- 可见内容：隐藏。
- 已隐藏或管理员已删除内容：恢复。
- 可见或已隐藏内容：删除。
- 置顶 / 取消置顶。
- 关闭回复 / 重新开放回复。
- 查看完整内容或上下文。

隐藏和删除时打开审核弹窗：
- 原内容摘要。
- 原因预设：广告营销、人身攻击、无关内容、违规信息、其他。
- 原因详情。
- 取消。
- 确认操作。

回复和弹幕标签页沿用相同的纵向内容卡片语言，但只显示各自适用字段与操作。操作审计页展示：
- 管理员 moderator.displayName。
- 目标类型 targetType。
- 目标 ID targetId。
- 操作 action。
- 原因 reason。
- 时间 createdAt。

所有内容列表底部显示总数和真实分页。不要加入公告编辑器、虚构举报队列、点赞量或没有数据合同的浏览量。

八、页面 5：用户管理

页面标题：
用户管理

页面说明：
集中管理用户资料、角色分配和邀请码，所有变更遵循现有权限规则。

不要只设计一个用户表格；必须保留三个一级标签页：
- 用户
- 角色
- 邀请码

标签上可以显示当前已加载数量。

A. 用户标签页

标题：
用户与角色

说明：
查看用户基本信息并调整所属角色；删除用户会同时移除其关联学习记录。

表格列：
- 用户：displayName
- 手机号：phone
- 外部用户 ID：hostUserId
- 当前角色：role.name 与 role.code
- 状态：status
- 操作

角色修改：
- 每行提供角色下拉框
- 每行单独“保存”
- 保存时显示“保存中”

危险操作：
- 永久删除用户
- 必须打开明确的危险确认弹窗
- 弹窗说明会清理学习进度、测评记录和阶段考核记录，且不可恢复

禁止显示：
- Email
- 最后登录时间
- IP
- 用户头像
- 新增用户
- 批量删除
- 批量更改角色
- 导出用户
- 封禁或解封按钮

允许在视觉稿中增加基于已加载数据的前端搜索框：
- 搜索显示名、手机号或外部用户 ID
但不要暗示存在新的服务端搜索 API。

B. 角色标签页

标题：
角色与权限

主操作：
- 创建角色

表格列：
- 角色名称 name
- 角色标识 code
- 类型：管理员角色 / 普通角色
- 当前用户数
- 当前邀请码数
- 操作

操作：
- 编辑
- 删除

创建和编辑弹窗字段：
- 角色名称 name
- 角色标识 code
- 是否管理员 isAdmin

删除弹窗必须说明：
- 至少保留一个管理员角色
- 已被用户、邀请码或阶段考核策略引用的角色可能无法删除

C. 邀请码标签页

标题：
邀请码

说明必须明确：
邀请码明文创建后不会在列表中再次展示，请自行安全保存。

主操作：
- 创建邀请码

表格列：
- 状态：启用中 / 已停用 / 已过期
- 绑定角色
- 创建时间 createdAt
- 过期时间 expiresAt
- 是否启用 enabled
- 操作

创建弹窗字段：
- 邀请码明文 code
- 绑定角色 roleId
- 过期时间 expiresAt
- 启用 enabled

编辑弹窗不得再次展示邀请码明文，只能修改：
- 绑定角色
- 过期时间
- 启用状态

操作：
- 编辑
- 撤销

撤销必须有不可恢复确认弹窗。

九、通用交互状态

五张图都要表现出一致的组件规则：

- 页面加载时使用骨架屏或明确的加载卡片。
- 接口失败时使用错误提示和“重新加载”按钮。
- 没有数据时使用空状态。
- 筛选没有结果时使用独立的“当前筛选没有结果”状态。
- 保存、发布、删除、审核过程中按钮进入 loading 状态并防止重复提交。
- 成功和失败使用顶部 Toast。
- 删除、撤销、隐藏等风险操作必须有确认弹窗。
- 表格行操作采用“一个主要按钮 + 三点更多菜单”。
- 所有图标按钮必须有可理解的文字或 Tooltip。
- 焦点状态清晰，文本和状态色满足可读性要求。
- 1280×1024 基准尺寸下页面不能产生横向滚动，扩展到 1440×900 和 1920×900 时也必须保持稳定。
- 课程管理在桌面端始终使用大封面卡片网格；其他表格在窄屏时可以切换为卡片，但不得丢失字段或操作。

十、最终自检

生成前逐页检查：

- 五张图是否使用同一个 Logo 和同一个品牌名称？
- 左侧导航是否完全一致？
- 顶部栏是否完全一致？
- 页面标题有没有被顶部栏遮挡？
- 是否保留“数据看板、课程管理、考核管理、社区内容、用户管理”五个模块？
- 用户管理是否保留“用户、角色、邀请码”三个标签页？
- 社区内容是否在顶部显示“今日新帖、今日评论、审核记录”三张卡片，并保留“帖子、回复、弹幕、操作审计”四个标签页？
- 考核管理是否使用“题库准备 + 考核通过率”两张并排卡片，并保留查看最近考核学员的入口？
- 考核管理是否仍然表现为考核策略管理，而不是人工批阅？
- 课程页是否使用大封面卡片，并包含分类管理、状态、可见范围、内容状态和发布/下架操作？
- 课程快捷筛选是否准确使用“全部、已发布、草稿、下架、待复核”？
- 数据看板是否准确展示“总学员数、活跃课程数、课程完成率、考核通过率”四张卡片？
- 数据看板下方是否按左侧社区活跃度折线图、右侧待处理事项完成 2:1 分栏，并支持周/月/年切换？
- 新增的社区趋势、社区统计、课程封面和最近考核学员数据是否明确使用正式数据合同，而不是写死演示值？
- 是否删除了 Email、IP、最后登录、公告、举报、人工批阅等未要求且没有数据合同的字段？
- 所有列表是否有总数、分页、空状态和错误状态？
